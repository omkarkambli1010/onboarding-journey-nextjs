import PageNotFound from '@/components/page-not-found/PageNotFound';

export const metadata = { title: 'Page Not Found | SBI Securities' };

export default function PageNotFoundPage() {
  return <PageNotFound />;
}
